spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/bloom
    username: ohgiraffers
    password: ohgiraffers
  servlet:
    multipart:
      enabled: true
      max-file-size : 10MB
      max-request-size: 10MB
      location: C:/bloom/
    devtools:
      restart:
        enabled: false
      livereload:
        enabled: true
  mail:
    host: smtp.gmail.com
    port: 587
    username: covbloomail@gmail.com
    password: terxdxpukxjodxus
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

image:
  image-dir : img

mybatis:
  mapper-locations: mybatis/mappers/**/*.xml